(def main (\ ()
  (do)))

(def quicksort (\ (xs)
    (and xs 
        (let (x (first xs)
              xs (rest xs))
          (concat (quicksort (filter (\ (n) (< n x)) xs))
                  (list x)
                  (quicksort (filter (\ (n) (>= n x)) xs)))))))


